/** Represents GPS coordinates of a location */
class GPSLocation {
	double latitude;  // degrees
	double longitude; // degrees
	
	GPSLocation(double latitude, double longitude) {
		this.latitude = latitude;
		this.longitude = longitude;
	}
}
